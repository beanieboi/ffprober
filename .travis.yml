language: ruby
cache: bundler

before_script:
  - sudo apt-get update -qq
  - sudo apt-get install -qq ffmpeg

rvm:
  - 1.9.3
  - 2.0.0
  - jruby-19mode
  - rbx
  - ruby-head

matrix:
  allow_failures:
    - rvm: rbx
